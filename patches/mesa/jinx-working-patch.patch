diff -urN --no-dereference mesa-clean/include/drm-uapi/drm.h mesa-workdir/include/drm-uapi/drm.h
--- mesa-clean/include/drm-uapi/drm.h
+++ mesa-workdir/include/drm-uapi/drm.h
@@ -44,7 +44,7 @@
 #else /* One of the BSDs */
 
 #include <stdint.h>
-#include <sys/ioccom.h>
+// #include <sys/ioccom.h>
 #include <sys/types.h>
 typedef int8_t   __s8;
 typedef uint8_t  __u8;
diff -urN --no-dereference mesa-clean/meson.build mesa-workdir/meson.build
--- mesa-clean/meson.build
+++ mesa-workdir/meson.build
@@ -127,7 +127,7 @@
 # Only build shared_glapi if at least one OpenGL API is enabled
 with_shared_glapi = with_shared_glapi and with_any_opengl
 
-system_has_kms_drm = ['openbsd', 'netbsd', 'freebsd', 'gnu/kfreebsd', 'dragonfly', 'linux', 'sunos', 'android', 'managarm'].contains(host_machine.system())
+system_has_kms_drm = ['openbsd', 'netbsd', 'freebsd', 'gnu/kfreebsd', 'dragonfly', 'linux', 'sunos', 'android', 'managarm', 'vinix'].contains(host_machine.system())
 
 gallium_drivers = get_option('gallium-drivers')
 if gallium_drivers.contains('auto')
@@ -1036,7 +1036,7 @@
 endif
 
 # TODO: this is very incomplete
-if ['linux', 'cygwin', 'gnu', 'freebsd', 'gnu/kfreebsd', 'haiku', 'android', 'managarm'].contains(host_machine.system())
+if ['linux', 'cygwin', 'gnu', 'freebsd', 'gnu/kfreebsd', 'haiku', 'android', 'managarm', 'vinix'].contains(host_machine.system())
   pre_args += '-D_GNU_SOURCE'
 elif host_machine.system() == 'sunos'
   pre_args += '-D__EXTENSIONS__'
diff -urN --no-dereference mesa-clean/src/compiler/spirv/spirv_to_nir.c mesa-workdir/src/compiler/spirv/spirv_to_nir.c
--- mesa-clean/src/compiler/spirv/spirv_to_nir.c
+++ mesa-workdir/src/compiler/spirv/spirv_to_nir.c
@@ -40,6 +40,7 @@
 #include "util/mesa-blake3.h"
 
 #include <stdio.h>
+#include <strings.h>
 
 /* Table of all implemented capabilities.  These are the capabilities that are
  * implemented in the spirv_to_nir, not what the device supports.
diff -urN --no-dereference mesa-clean/src/egl/main/egllog.c mesa-workdir/src/egl/main/egllog.c
--- mesa-clean/src/egl/main/egllog.c
+++ mesa-workdir/src/egl/main/egllog.c
@@ -37,6 +37,7 @@
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
+#include <strings.h>
 #include "c11/threads.h"
 #include "util/macros.h"
 #include "util/os_misc.h"
diff -urN --no-dereference mesa-clean/src/gallium/drivers/llvmpipe/lp_texture.c mesa-workdir/src/gallium/drivers/llvmpipe/lp_texture.c
--- mesa-clean/src/gallium/drivers/llvmpipe/lp_texture.c
+++ mesa-workdir/src/gallium/drivers/llvmpipe/lp_texture.c
@@ -1741,7 +1741,7 @@
    default:
       break;
    }
-   assert(0);
+   // assert(0);
    *value = 0;
    return false;
 }
diff -urN --no-dereference mesa-clean/src/util/detect_os.h mesa-workdir/src/util/detect_os.h
--- mesa-clean/src/util/detect_os.h
+++ mesa-workdir/src/util/detect_os.h
@@ -28,6 +28,12 @@
 #define DETECT_OS_ANDROID 1
 #endif
 
+#if defined(__vinix__)
+#define DETECT_OS_VINIX 1
+#define DETECT_OS_UNIX 1
+#define DETECT_OS_POSIX 1
+#endif
+
 #if defined(__FreeBSD__) || defined(__FreeBSD_kernel__)
 #define DETECT_OS_FREEBSD 1
 #define DETECT_OS_BSD 1
@@ -147,4 +153,8 @@
 #define DETECT_OS_MANAGARM 0
 #endif
 
+#ifndef DETECT_OS_VINIX
+#define DETECT_OS_VINIX 0
+#endif
+
 #endif /* DETECT_OS_H */
diff -urN --no-dereference mesa-clean/src/util/os_misc.c mesa-workdir/src/util/os_misc.c
--- mesa-clean/src/util/os_misc.c
+++ mesa-workdir/src/util/os_misc.c
@@ -57,7 +57,7 @@
 #  include <unistd.h>
 #  include <log/log.h>
 #  include <cutils/properties.h>
-#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM
+#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM || DETECT_OS_VINIX
 #  include <unistd.h>
 #elif DETECT_OS_OPENBSD || DETECT_OS_FREEBSD
 #  include <sys/resource.h>
@@ -284,7 +284,7 @@
 bool
 os_get_total_physical_memory(uint64_t *size)
 {
-#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM
+#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM || DETECT_OS_VINIX
    const long phys_pages = sysconf(_SC_PHYS_PAGES);
    const long page_size = sysconf(_SC_PAGE_SIZE);
 
diff -urN --no-dereference mesa-clean/src/util/os_time.c mesa-workdir/src/util/os_time.c
--- mesa-clean/src/util/os_time.c
+++ mesa-workdir/src/util/os_time.c
@@ -65,7 +65,7 @@
 void
 os_time_sleep(int64_t usecs)
 {
-#if DETECT_OS_LINUX || DETECT_OS_MANAGARM || DETECT_OS_FUCHSIA
+#if DETECT_OS_LINUX || DETECT_OS_MANAGARM || DETECT_OS_VINIX || DETECT_OS_FUCHSIA
    struct timespec time;
    time.tv_sec = usecs / 1000000;
    time.tv_nsec = (usecs % 1000000) * 1000;
diff -urN --no-dereference mesa-clean/src/util/u_debug.c mesa-workdir/src/util/u_debug.c
--- mesa-clean/src/util/u_debug.c
+++ mesa-workdir/src/util/u_debug.c
@@ -32,6 +32,7 @@
 #include "util/u_math.h"
 #include <inttypes.h>
 
+#include <strings.h>
 #include <stdio.h>
 #include <limits.h> /* CHAR_BIT */
 #include <ctype.h> /* isalnum */
diff -urN --no-dereference mesa-clean/src/util/u_thread.c mesa-workdir/src/util/u_thread.c
--- mesa-clean/src/util/u_thread.c
+++ mesa-workdir/src/util/u_thread.c
@@ -75,7 +75,7 @@
 void u_thread_setname( const char *name )
 {
 #if defined(HAVE_PTHREAD)
-#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || defined(__GLIBC__) || DETECT_OS_MANAGARM || DETECT_OS_FUCHSIA
+#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || defined(__GLIBC__) || DETECT_OS_MANAGARM || DETECT_OS_VINIX || DETECT_OS_FUCHSIA
    int ret = pthread_setname_np(pthread_self(), name);
    if (ret == ERANGE) {
       char buf[16];
@@ -154,7 +154,7 @@
 int64_t
 util_thread_get_time_nano(thrd_t thread)
 {
-#if defined(HAVE_PTHREAD) && !defined(__APPLE__) && !defined(__HAIKU__) && !defined(__managarm__)
+#if defined(HAVE_PTHREAD) && !defined(__APPLE__) && !defined(__HAIKU__) && !defined(__managarm__) && !defined(__vinix__)
    struct timespec ts;
    clockid_t cid;

diff -urN --no-dereference mesa-clean/src/loader/loader.c mesa-workdir/src/loader/loader.c
--- mesa-clean/src/loader/loader.c
+++ mesa-workdir/src/loader/loader.c
@@ -258,6 +258,18 @@
 bool
 loader_is_device_render_capable(int fd)
 {
+#ifdef __vinix__
+   /* Vinix exposes render nodes without Linux sysfs.  libdrm's generic
+    * drmGetDevice2() therefore cannot reconstruct a drmDevice from st_rdev,
+    * even though the fd is a fully functional DRM render node. */
+   drmVersionPtr version = drmGetVersion(fd);
+   if (!version)
+      return false;
+
+   bool ret = strcmp(version->name, "asahi") == 0;
+   drmFreeVersion(version);
+   return ret;
+#else
    drmDevicePtr dev_ptr;
    bool ret;
 
@@ -269,6 +281,7 @@
    drmFreeDevice(&dev_ptr);
 
    return ret;
+#endif
 }
 
 char *
diff -urN --no-dereference mesa-clean/src/egl/main/egldevice.c mesa-workdir/src/egl/main/egldevice.c
--- mesa-clean/src/egl/main/egldevice.c
+++ mesa-workdir/src/egl/main/egldevice.c
@@ -174,6 +174,14 @@
    if (software)
       goto out;
 
+#ifdef __vinix__
+   /* There is one native AGX render node today.  It was inserted by
+    * _eglDeviceRefreshList(), so no sysfs-backed drmGetDevice2 lookup is
+    * needed to match this already-open fd to its EGLDevice. */
+   dev = dev->Next;
+   goto out;
+#endif
+
 #ifdef HAVE_LIBDRM
    drmDevicePtr device;
 
@@ -314,6 +322,34 @@
    count++;
 
 #ifdef HAVE_LIBDRM
+#ifdef __vinix__
+   /* libdrm enumerates through Linux sysfs.  Vinix deliberately has no fake
+    * sysfs hierarchy, so describe its stable first render node directly. */
+   if (dev->Next)
+      return count + 1;
+
+   drmDevicePtr device = calloc(1, sizeof(*device));
+   if (!device)
+      return count;
+
+   device->nodes = calloc(DRM_NODE_MAX, sizeof(*device->nodes));
+   if (!device->nodes) {
+      free(device);
+      return count;
+   }
+
+   device->nodes[DRM_NODE_RENDER] = strdup("/dev/dri/renderD128");
+   if (!device->nodes[DRM_NODE_RENDER]) {
+      drmFreeDevice(&device);
+      return count;
+   }
+   device->available_nodes = 1 << DRM_NODE_RENDER;
+   device->bustype = DRM_BUS_PLATFORM;
+   if (_eglAddDRMDevice(device) >= 0)
+      count++;
+   else
+      drmFreeDevice(&device);
+#else
    drmDevicePtr devices[64];
    int num_devs, ret;
 
@@ -336,6 +368,7 @@
       if (ret >= 0)
          count++;
    }
+#endif
 #endif
 
    return count;
diff -urN --no-dereference mesa-clean/src/gallium/auxiliary/target-helpers/drm_helper_public.h mesa-workdir/src/gallium/auxiliary/target-helpers/drm_helper_public.h
--- mesa-clean/src/gallium/auxiliary/target-helpers/drm_helper_public.h
+++ mesa-workdir/src/gallium/auxiliary/target-helpers/drm_helper_public.h
@@ -23,6 +23,7 @@
 extern const struct drm_driver_descriptor tegra_driver_descriptor;
 extern const struct drm_driver_descriptor lima_driver_descriptor;
 extern const struct drm_driver_descriptor zink_driver_descriptor;
+extern const struct drm_driver_descriptor asahi_driver_descriptor;
 extern const struct drm_driver_descriptor kmsro_driver_descriptor;
 
 #endif /* _DRM_HELPER_PUBLIC_H */
diff -urN --no-dereference mesa-clean/src/gallium/auxiliary/target-helpers/drm_helper.h mesa-workdir/src/gallium/auxiliary/target-helpers/drm_helper.h
--- mesa-clean/src/gallium/auxiliary/target-helpers/drm_helper.h
+++ mesa-workdir/src/gallium/auxiliary/target-helpers/drm_helper.h
@@ -420,6 +420,25 @@
 DRM_DRIVER_DESCRIPTOR_STUB(zink)
 #endif
 
+#ifdef GALLIUM_ASAHI
+#include "asahi/drm/asahi_drm_public.h"
+
+static struct pipe_screen *
+pipe_asahi_create_screen(int fd, const struct pipe_screen_config *config)
+{
+   struct pipe_screen *screen = asahi_drm_screen_create(fd, config);
+   return screen ? debug_screen_wrap(screen) : NULL;
+}
+
+const driOptionDescription asahi_driconf[] = {
+      #include "asahi/driinfo_asahi.h"
+};
+DRM_DRIVER_DESCRIPTOR(asahi, asahi_driconf, ARRAY_SIZE(asahi_driconf))
+
+#else
+DRM_DRIVER_DESCRIPTOR_STUB(asahi)
+#endif
+
 #ifdef GALLIUM_KMSRO
 #include "kmsro/drm/kmsro_drm_public.h"
 
diff -urN --no-dereference mesa-clean/src/gallium/auxiliary/pipe-loader/pipe_loader_drm.c mesa-workdir/src/gallium/auxiliary/pipe-loader/pipe_loader_drm.c
--- mesa-clean/src/gallium/auxiliary/pipe-loader/pipe_loader_drm.c
+++ mesa-workdir/src/gallium/auxiliary/pipe-loader/pipe_loader_drm.c
@@ -92,6 +92,7 @@
    &tegra_driver_descriptor,
    &lima_driver_descriptor,
    &zink_driver_descriptor,
+   &asahi_driver_descriptor,
 };
 #endif
 
diff -urN --no-dereference mesa-clean/src/asahi/libagx/meson.build mesa-workdir/src/asahi/libagx/meson.build
--- mesa-clean/src/asahi/libagx/meson.build
+++ mesa-workdir/src/asahi/libagx/meson.build
@@ -29,7 +29,8 @@
     '-fmacro-prefix-map=@0@='.format(relative_dir),
     ],
   env: ['MESA_SHADER_CACHE_DISABLE=true'],
-  depends : [prog_mesa_clc, agx_pack],
+  depends : [agx_pack] + (get_option('mesa-clc') == 'system' ?
+                          [] : [prog_mesa_clc]),
   depfile : 'libagx_shaders.h.d',
 )
 
@@ -38,7 +39,8 @@
   input : libagx_spv,
   output : 'libagx.h',
   command : [prog_vtn_bindgen, libagx_spv, '@OUTPUT@'],
-  depends : [prog_vtn_bindgen],
+  depends : get_option('mesa-clc') == 'system' ?
+            [] : [prog_vtn_bindgen],
 )
 
 idep_libagx = declare_dependency(
diff -urN --no-dereference mesa-clean/src/asahi/lib/meson.build mesa-workdir/src/asahi/lib/meson.build
--- mesa-clean/src/asahi/lib/meson.build
+++ mesa-workdir/src/asahi/lib/meson.build
@@ -56,7 +56,8 @@
   output : ['libagx_shaders.h', 'libagx_shaders.c'],
   command : [prog_asahi_clc, libagx_spv, '@OUTPUT0@', '@OUTPUT1@'],
   env: ['MESA_SHADER_CACHE_DISABLE=true'],
-  depends : [prog_asahi_clc],
+  depends : get_option('precomp-compiler') == 'system' ?
+            [] : [prog_asahi_clc],
 )
 
 idep_libagx_shaders_h = declare_dependency(
diff -urN --no-dereference mesa-clean/src/gallium/drivers/asahi/meson.build mesa-workdir/src/gallium/drivers/asahi/meson.build
--- mesa-clean/src/gallium/drivers/asahi/meson.build
+++ mesa-workdir/src/gallium/drivers/asahi/meson.build
@@ -23,7 +23,7 @@
   include_directories : [inc_gallium_aux, inc_gallium, inc_include, inc_src, inc_asahi, inc_virtio_gpu, inc_virtio_vdrm],
   c_args : [c_msvc_compat_args, '-Wno-c2x-extensions'],
   gnu_symbol_visibility : 'hidden',
-  dependencies : [idep_nir, idep_mesautil, idep_agx_pack, dep_libdrm, idep_mesaclc, idep_libagx, idep_libagx_shaders_h],
+  dependencies : [idep_nir, idep_mesautil, idep_agx_pack, dep_libdrm, idep_libagx, idep_libagx_shaders_h],
 )
 
 driver_asahi = declare_dependency(

diff -urN --no-dereference mesa-clean/src/gallium/drivers/asahi/agx_pipe.c mesa-workdir/src/gallium/drivers/asahi/agx_pipe.c
--- mesa-clean/src/gallium/drivers/asahi/agx_pipe.c
+++ mesa-workdir/src/gallium/drivers/asahi/agx_pipe.c
@@ -2583,13 +2583,6 @@
    struct agx_screen *agx_screen;
    struct pipe_screen *screen;
 
-   /* Refuse to probe. There is no stable UAPI yet. Upstream Mesa cannot be used
-    * yet with Asahi. Do not try. Do not patch out this check. Do not teach
-    * others about patching this check. Do not distribute upstream Mesa with
-    * this check patched out.
-    */
-   return NULL;
-
    agx_screen = rzalloc(NULL, struct agx_screen);
    if (!agx_screen)
       return NULL;
