name=kernel
from_source=kernel
revision=1
hostdeps="gcc v"

build() {
    cp -rpf "${source_dir}"/. ./

    make -j${parallelism} TOOLCHAIN_PREFIX=x86_64-vinix-
}

package() {
    make install PREFIX="${prefix}" DESTDIR="${dest_dir}"

    post_package_strip
}
