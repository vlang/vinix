name=libtool
from_source=libtool
revision=1
hostdeps="autoconf automake"
imagedeps="help2man gcc"

build() {
    cp -r "${source_dir}"/. ./
    ./configure \
        --prefix="${prefix}"
    make -j${parallelism}
}

install() {
    DESTDIR="${dest_dir}" make install-strip
}
