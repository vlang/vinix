Some tools meant to be the `util-linux` equivalent for
[vinix](https://github.com/vlang/vinix).

## Building

util-vinix uses a makefile, `make` and `make install` will suffice.

## Implemented tools

- `chsh`
- `fetch`
